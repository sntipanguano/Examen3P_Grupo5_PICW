@inherits LayoutComponentBase

<div class="page-container">
    <div class="header">
        <img src="images/logo.png" alt="Logo de la aplicación" />
    </div>

    <div class="navbar">
        <!-- Barra de navegación aquí (por ejemplo, usando NavLink) -->
        <NavLink href="/">Inicio</NavLink>
        <NavLink href="/viajes">Viajes</NavLink>
        <NavLink href="/destinos">Destinos</NavLink>
    </div>

    <div class="main-content">
        @Body
    </div>
</div>

<div class="footer">
    <div class="footer-content">
        <div class="social-media">
            <a href="https://www.instagram.com/tu_usuario_de_instagram">
                <img src="images/logo.png" alt="Instagram" />
            </a>
        </div>

        <div class="address">
            <p>Universidad de las Fuerzas Armadas ESPE, Quito, Ecuador</p>
            <p>Teléfono: +5939876544312</p>
        </div>
    </div>
</div>

<style>
    .header {
        background-color: whitesmoke; /* Cambia el color de fondo según tu diseño */
        padding: 20px;
        text-align: center;
    }
    
    .header img {
        max-width: 90px; /* Ajusta el tamaño máximo de la imagen */
    }
    
    /* Estilos adicionales para la barra de navegación, si es necesario */
    .navbar {
        background-color: #5dc;
        padding: 10px;
        display: flex;
        justify-content: center; /* Centra horizontalmente el contenido */
        align-items: center; /* Centra verticalmente el contenido */
    }
    
    .navbar a {
        color: white;
        text-decoration: none;
        margin: 0 30px; /* Espaciado entre los enlaces */
        font-family: 'Lucida Bright';
    }

    .navbar a {
        font-size: 18px;
        color: #333;
        transition: color 0.3s; /* Agrega una transición suave al cambio de color */
    }

    .navbar a:hover {
        color: #ff9900; /* Cambia el color al pasar el cursor */
    }
    
    /* Estilos para el footer */
    .footer {
        background-color: #5dc1b9;
        color: white;
        padding: 20px;
        text-align: center;
    }
    
    .social-media img {
        max-width: 30px;
        margin: 0 10px; /* Espaciado entre iconos de redes sociales */
    }
    
    html, body {
        height: 100%;
        margin: 0;
        padding: 0;
    }
    
    /* Contenedor de la página */
    .page-container {
        display: flex;
        flex-direction: column;
        min-height: 100vh; /* Al menos 100% del alto de la pantalla */
    }
    
    /* Contenido principal de la página (excluyendo el footer) */
    .main-content {
        flex: 1; /* Toma todo el espacio restante */
    }
    
    .image-with-text-container {
        display: flex;
        align-items: center; /* Centra verticalmente el contenido */
    }
    
    .image-container {
        flex: 1; /* Ocupa el espacio restante */
        padding: 10px; /* Espaciado entre la imagen y el texto */
    }
    
    .text-container {
        flex: 1; /* Ocupa el espacio restante */
        padding: 10px; /* Espaciado entre la imagen y el texto */
        background-color: #f5f5f5; /* Color de fondo del lado del texto */
    }

    

</style>